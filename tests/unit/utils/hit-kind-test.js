import hitKind from 'ipfs-search/utils/hit-kind';
import { module, test } from 'qunit';

module('Unit | Utility | hit-kind', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = hitKind();
    assert.ok(result);
  });
});
